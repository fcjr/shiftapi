{
  "name": "{{name}}",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "postinstall": "shiftapi prepare"
  },
  "devDependencies": {
    "@shiftapi/core": "^{{version}}"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
